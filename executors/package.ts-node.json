{
  "name": "delven-explorer-server",
  "version": "2.0.0",
  "type": "module",
  "main": "index.js",
  "private": true,
  "author": "Greg",
  "dependencies": {},
  "scripts": {
    "start": "npm run build && node -r tsconfig-paths/register build/index.js",
    "check-types": "tsc",
    "watch-ts": "tsc -w --outDir ./build",
    "dev": "nodemon",
    "build": "rm -rf ./build && mkdir ./build && npx tsc"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "ts-node": "^9.0.0",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.0.3"
  }
}
